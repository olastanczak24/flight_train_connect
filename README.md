# flight_train_connect
This project is a train and flight connection scheduler. It helps visualize the travel connections between different cities, both by train and flight, based on a set of predefined connections. The data is stored in a connections.json file and can be visualized using Python scripts.
